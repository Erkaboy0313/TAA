# TAA — Faza 1 dependencies (E00-S01 + E00-S02 scope).
# Every entry justified. New deps require PR justification (project-context R6).

# Web framework — 5.1 LTS: familiar (IWALLET), sync/async mix, batteries included.
Django>=5.1,<5.2

# Env-driven single settings.py (project-context R6). Loads .env → config().
python-decouple>=3.8

# DATABASE_URL parsing — simpler than manual parse; used in settings.py.
dj-database-url>=2.3

# Postgres driver (psycopg 3.x, binary wheel — no build toolchain needed locally).
# pgvector extension itself is enabled at DB-init in E00-S02, not here.
psycopg[binary]>=3.2

# ASGI server for async views (voice/RAG endpoints in later stories).
# Installed now so `python manage.py check --deploy` and dev runserver both work.
uvicorn[standard]>=0.32

# Background task queue (E00-S02). Required by celery-worker/celery-beat
# containers. Actual periodic tasks (lex.uz weekly sync, deadline pushes)
# land in E02-S06 and E10-S04.
celery>=5.4,<5.5

# Python client for Redis — Celery broker/result-backend transport
# (E00-S02). Also used by rate-limit middleware (E05-S07) in later stories.
redis>=5.0,<6.0

# Telegram bot SDK. Webhook mode; async — matches R9 voice endpoints.
python-telegram-bot>=21.6,<22.0

# Gemini SDK — unified STT + TTS + LLM provider (architecture §5). Upper bound
# stays broad while the SDK matures; API surface used here is stable.
google-genai>=0.7,<3.0

# Async HTTP client used underneath the Gemini SDK for streaming responses
# (architecture §5). Also the retry target for tenacity — the SDK raises
# httpx.HTTPError on transient network failures.
httpx>=0.28,<0.29

# Retry with exponential backoff for Gemini transient failures (architecture
# §5, R9: 3x exp backoff, 30s overall timeout). Only used at the Gemini
# boundary — R6 forbids adding retry helpers elsewhere.
tenacity>=9.0,<10.0

# pgvector Django integration — VectorField + HnswIndex + VectorExtension migration.
# Postgres side of the extension is enabled by the E02-S01 initial migration
# (architecture §4). Vector store choice justified in project-context §2.
pgvector>=0.3,<1.0

# lex.uz Tax Code scraper (E02 real ingest). BeautifulSoup drives the DOM
# walk; lxml is the fast C-backed parser it delegates to. Both are confined
# to `apps.corpus.scrapers` — R6 forbids adding HTML parsing elsewhere.
beautifulsoup4>=4.12,<5.0
lxml>=5.3,<6.0
